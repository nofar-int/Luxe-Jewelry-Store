FROM node:18
WORKDIR /app

# העתקת package.json והתקנת dependencies
COPY auth/package*.json ./
RUN npm install

# העתקת הקוד
COPY auth/. .

# חשיפת פורט 4000 (או מה שהאפליקציה שלך משתמשת)
EXPOSE 4000

# הפעלת ה-service
CMD ["npm", "start"]


