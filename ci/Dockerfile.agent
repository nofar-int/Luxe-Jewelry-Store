FROM jenkins/agent:latest

USER root

# התקנת docker-cli ו-git פעם אחת
RUN apt-get update && \
    apt-get install -y docker.io git && \
    rm -rf /var/lib/apt/lists/*

# נרד חזרה למשתמש jenkins
USER jenkins

WORKDIR /home/jenkins
